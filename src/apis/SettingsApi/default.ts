import { Settings } from './types';

export const defaultSettings: Settings = {
  loading: false,
  palette: 'light',
  iconColor: 'blue',
  fontSize: '14px',
  lineHeight: '1.5',
  noWrap: true,
  escapeBehavior: 'clear',
  defaultOpenMap: {},
};
